name: Build and Publish to Docker Hub

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout código
      uses: actions/checkout@v4

    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: maven

    - name: Build with Maven
      run: mvn -B package --file pom.xml

    # --- NOVOS PASSOS PARA O DOCKER ---

    - name: Log in to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}

    - name: Build and push Docker image
      uses: docker/build-push-action@v5
      with:
        context: .
        file: ./Dockerfile
        push: true
        # Substitua 'meu-usuario-docker' e 'nome-do-app' pelos seus dados se preferir fixar,
        # ou use a variável abaixo que pega automaticamente seu usuário das Secrets.
        tags: |
          ${{ secrets.DOCKERHUB_USERNAME }}/api-bitcoin:latest
          ${{ secrets.DOCKERHUB_USERNAME }}/api-bitcoin:${{ github.sha }}
